import { View, Text } from 'react-native';
import { Flex } from 'native-base';

const Home = () => {
  return (
    <View>
      <Flex>
        <Text>Filtros</Text>
        <Text>Lista</Text>
      </Flex>
    </View>
  );
};

export default Home;
